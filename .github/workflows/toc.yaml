name: TOC Generator

on:
  push:
    paths:
      - 'README.md'

jobs:
  toc:
    permissions:
      contents: write
    uses: Lx34r/GitHubActions/.github/workflows/_toc-generator.yaml@main
    with:
      TOC_TITLE: "## Table of Contents"
    secrets: inherit
